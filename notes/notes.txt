https://chessboardjs.com/index.html

https://github.com/jhlywa/chess.js

npx nodemon server.js

brew services start mongodb-community

brew services stop mongodb-community

db.users.insert({id: 210041,
    move: {
        fen: 'rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1',
})


db.users.update({ id: 210041 },
{
  $set: {
    dateTime: Date()
  }
})





let date_ob = new Date();

// current date
// adjust 0 before single digit date
let date = ("0" + date_ob.getDate()).slice(-2);

// current month
let month = ("0" + (date_ob.getMonth() + 1)).slice(-2);

// current year
let year = date_ob.getFullYear();

// current hours
let hours = date_ob.getHours();

// current minutes
let minutes = date_ob.getMinutes();

// current seconds
let seconds = date_ob.getSeconds();

// prints date in YYYY-MM-DD format
console.log(year + "-" + month + "-" + date);

// prints date & time in YYYY-MM-DD HH:MM:SS format
console.log(year + "-" + month + "-" + date + " " + hours + ":" + minutes + ":" + seconds);

// prints time in HH:MM format
console.log(hours + ":" + minutes);




Send get request from within main.js file to get current board position

Check move on server




db.users.update(
        {email: 'stie4966@lgsstudent.org'},
        { $addToSet: { tags: "poggers" } },
)

db.users.update(
         {email: 'stie4966@lgsstudent.org'},
         { $addToSet: { tags: "poggers" } },
         {upsert: true}
)

db.users.update({
    email: 'stie4966@lgsstudent.org',
    name: "Eric Still",
    userId: 42068
    },
    {$addToSet: {tags: "poggers"}},
    {upsert: true}
)

if tags does not exist then it is created and poggers is added to it
if tags does exist then poggers is added to it

profile = googleUser.getBasicProfile();
console.log('ID: ' + profile.getId()); // Do not send to your backend! Use an ID token instead.
console.log('Name: ' + profile.getName());
console.log('Image URL: ' + profile.getImageUrl());
console.log('Email: ' + profile.getEmail()); // This is null if the 'email' scope is not present.